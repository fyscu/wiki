---
title: ping 分层诊断与浏览器内核
doc_id: 01m1zwvt7kee904w3hc30czz8x
manual_pages:
  - 115
  - 123
owners:
  - maintenance-team
tags:
  - 网络
  - ping
  - 浏览器
updated: 2026-09-08T00:00:00.000Z
---
# ping 分层诊断与浏览器内核

<!-- PDF 第 115 页；印刷第 114 页 -->

### 第五节：宽带连接错误

当网络由于各种各样原因无法连接时，拨号器就会报错，这样的错误经常以代码的形式给出。而面对这样的几个数字时，操作者往往会一头雾水，上网百度？可是面对网络上密密麻麻的几百行文字，不免心底怕怕。

其实只要知道几个常见的宽带错误代码，我们就可以轻松地解决问题，下面就让我们看一下都有什么吧？以下列出了一些常见错误代码和相应的可行的解决方法。

- 605 不能设置端口信息方法：删除并重新创建“ 网络和拨号连接” ；

![原手册插图（PDF 第 115 页）](/images/manual/pdf-p115-i00.jpg)

![原手册插图（PDF 第 115 页）](/images/manual/pdf-p115-i01.jpg)

![原手册插图（PDF 第 115 页）](/images/manual/pdf-p115-i02.jpg)

<!-- PDF 第 116 页；印刷第 115 页 -->

- 612 指定的路由未分配方法：网络配置可能不正确，重新启动计算机即可；− 615 未找到指定的端口方法： 重新配置“ 网络和拨号连接”， 如果错误仍然存在， 删除并重新创建“ 网络和拨号连接”；− 619 指定的端口未连接方法：重新启动计算机；− 620 无法决定端点方法：网络配置可能不正确，重新启动计算机，如果错误仍然存在，请参照事件查看器，查找详细的警告或错误；− 630 没有合适的网卡或驱动方法：检查网卡是否工作，网络线路是否插好，重新安装网卡驱动程序并确定网卡正常工作；− 651 由于用户端原因 （ 拨号软件、网卡驱动、 计算机系统、网线等）或运营商部分（ 局端设备、设备端口），多数情况下由于外部网络断线，或者设备出现了问题导致不通；

#### 方法：

1. 网线没接上，重新插拔网线或更换网线；

2. 如果是突然出现的，则是系统故障：替换系统文件，将系统中 c：\windows\system32\drivers 目录下的 raspppoe.sys 文件从一台正常拨号的电脑中复制并替换；

3. 网卡驱动问题，在设备管理器里将网卡驱动卸载，重启电脑， windows会自动安装自带的驱动；

4. 重装系统，没有办法的办法。

- 678 宽带线路问题

<!-- PDF 第 117 页；印刷第 116 页 -->

方法： 关闭路由器或猫，把所有线路重新插拔一下，等 3~5 分钟开路由，开电脑，再连接宽带；− 691 由于域上的用户名或密码无效而拒绝访问

#### 方法：

1. 用户名密码填写错误，重新填写即可；

2. 宽带欠费，请及时续费；− 769 本地连接或网卡出现问题， 可能是本地连接被禁用、 网卡松动、 网卡驱动丢失造成的

#### 方法：

1. 如果是本地连接被禁用，双击启动即可；

2. 网卡问题，更新驱动后如果还不行，就是网卡坏了，换网卡。网络错误代码大致是这样，其实如果我们不想自己检查的话，遇到网络连接其实试一试 360 断网急救箱其实一般就可以解决了。

### 第六节：PING 的介绍及操作

ping 在 windows 系统下是自带的一个可执行命令，利用它可以检查网络是否连通，并可以很好地帮助我们分析判定网络故障。其是一个通信协议，是 TCP/IP 协议的一部分。其基本应用格式为：ping 地址，方法为开始➔运行➔cmd。

并且 Ping 不仅是windows 下的命令，在 unix 系统和 linux 系统下也有这个命令，下面让我们来一探ping 的庐山真面目！

#### 一、 Ping 的概念

Ping（Packet Internet Groper），因特网包探索器，用于测试网络连接量的程序。Ping 是一个 DOS 命令，一般用于检测网络通与不通，也叫时延，其值越大，速度越慢。

<!-- PDF 第 118 页；印刷第 117 页 -->

#### 二、 Ping 的工作原理

利用网络上机器 IP 地址的唯一性，给目标 IP 地址发送一个数据包，在要求对方返回一个同样大小的数据包来确定两台网络机器是否相同，时延是多少。Ping 指的是端对端连通，通常用来作为可用性检查，但某些病毒木马会强行大量远程执行 ping 命令抢占机器的网络资源，导致系统变慢，网速变慢。

#### 三、 Ping 的操作

连通问题是由许多原因引起的，如本地配置错误、远程主机协议失效等，当然还包括

#### 设备等造成的故障。使用 ping 命令检查连通性有六个步骤：

1. 使用 ipconfig/all 观察本地网络设置是否正确

2. ping 127.0.0.1 回送地址， ping 回送地址是为了检查本地的 TCP/IP 协议有没有设置好；

![原手册插图（PDF 第 118 页）](/images/manual/pdf-p118-i00.png)

<!-- PDF 第 119 页；印刷第 118 页 -->

3. ping 本机 IP 地址，这样是为了检查本机的 IP 地址设置是否有误；

4. ping 本网网关或本网 IP 地址，这样是为了检查硬件设备是否有问题，也可以检查本机与本地网络连接是否正常，在非局域网中这一步骤可以省略；

5. ping 本地 DNS 地址，这样做是为了检查 DNS 是否能够将域名转换为 IP 地址；

![原手册插图（PDF 第 119 页）](/images/manual/pdf-p119-i00.png)

![原手册插图（PDF 第 119 页）](/images/manual/pdf-p119-i01.png)

<!-- PDF 第 120 页；印刷第 119 页 -->

6. ping 远程 IP 地址，这主要是检查本网或本机与外部连接是否正常。

#### 四、 Ping 操作的返回错误

在检查网络连通的过程中可能出现一些错误，这些错误总的来说有两种最常见。

1. reguest timed out也称请求超时，这个提示可能是由于对方可能装有防火墙或已关机，或者由于本机 IP 不正确和网关设置错误。IP 地址不正确主要是 IP 地址设置错误或 IP 地址冲突，这可以利用 pingipconfig/all 来检查；网关设置错误主要是 网关地址设置不正确或网关没能帮你转发数据， 还有可能是远程网关失效。

![原手册插图（PDF 第 120 页）](/images/manual/pdf-p120-i00.png)

![原手册插图（PDF 第 120 页）](/images/manual/pdf-p120-i01.png)

<!-- PDF 第 121 页；印刷第 120 页 -->

2. destination host unreachable如果局域网中使用 DHCP 分配 IP 时，而碰巧 DHCP 失效，这是 ping 命令就会产生此错误，因为 DHCP 失效时客户机无法分配到 IP，此时系统只好自设 IP，它往往不同于子网的 IP，于是出现此错误。另外子网掩码设置错误也会出现此错误。

#### 五、 ping 命令小技巧

“.0”可以有条件省略在使用“ping 127.0.0.1”命令来验证本机 TCP/IP 协议是否被正确安装时，发现使用“ping 127.1”也能得到相同结果，为什么会这样呢？这是因为其实这两条命令是相同的，IP 地址是由 32 位二进制数组成的，为了方便记忆，把每 8 位二进制数换算成十进制数，从而构成常见的 IP 地址（如 127.0.0.1），由于 windows 系统具有自动填充“.0”的功能，所以“127.0.0.1”就可以变为“127.1”。但是这种省略是有条件的，在 ping 命令中， 只能将在 IP 地址的最后一个十进制数前出现的一个或多个“.0”省略，也就是说“202.0.9.1”不能写成“202.9.1”。

![原手册插图（PDF 第 121 页）](/images/manual/pdf-p121-i00.jpg)

![原手册插图（PDF 第 121 页）](/images/manual/pdf-p121-i01.png)

<!-- PDF 第 122 页；印刷第 121 页 -->

### 第七节：浏览器内核及常见浏览器

浏览器是我们网络生活中离不开的一个工具，现在就让我们来探一探浏览器的面目吧。浏览器最重要或者说核心的部分是“Rendering Engine”，可大概译为“解释引擎”， 不过我们一般习惯将之称为“浏览器内核”。负责对网页语法的解释（如HTML、JavaScript）并渲染（显示）网页。

所以，通常所谓的浏览器内核也就是浏览器所采用的渲染引擎，渲染引擎决定了浏览器如何显示网页的内容以及页面的格式信息。不同的浏览器内核对网页编写语法的解释也有不同，因此同一网页在不同的内核的浏览器里的渲染（显示）效果也可能不同，这也是网页编写者需要在不同内核的浏览器中测试网页显示效果的原因。浏览器内核很多， 若加上几乎没人用的内核， 可能有 10 款甚至更多，不过常见的只有以下 4 种。

我们把使用自己研发的内核的常见浏览器称主流浏览器，它们是(按流行程度排序)Google Chrome、 Windows Internet Explorer、 Mozilla Firefox 和Apple Safari、Opera刚刚从主流浏览器变成壳浏览器(壳浏览器指使用别人研发的内核的浏览器)。国内常见的浏览器还有搜狗高速浏览器、腾讯 TT、 360 安全浏览器、 360 极速浏览器、傲游浏览器。这些浏览器的内核几乎一致为 Webkit（极速）和 Trident（普通）两种，所以着重介绍一下这些浏览器内核。

Trident（IE 内核）：该内核程序在 1997 年的 IE4 中首次被采用，是微软在 Mosaic代码的基础之上修改而来的，并沿用到 IE11。Trident 实际上是一款开放的内核，其接口内核设计的相当成熟，因此才有许多采用 IE 内核而非 IE 的浏览器涌现 （ 如 360浏览器、 Maxthon、The World、 TT、Green Browser、Avant Browser 等）。此外，为了方便也有很多人直接简称其为 IE 内核（当然也不排除有部分人是因为不知道内核名称而只好如此说）。

Webkit（Safari）：苹果公司自己的内核，也是苹果的 Safari 浏览器使用的内核。Webkit 引擎包含 Web Core 排版引擎及 Java Script Core 解析引擎，均是从 KDE 的KHTML 及 KJS 引擎衍生而来，它们都是自由软件，在 GPL 条约下授权，同时支持BSD 系统的开发。所以 Webkit 也是自由软件， 同时开放源代码。

在安全方面不受IE、 Firefox 的制约，所以 Safari 浏览器在国内还是很安全的。

<!-- PDF 第 123 页；印刷第 122 页 -->

Blink（Chrome） ：由 Google 和 Opera Software 开发的浏览器排版引擎，自2013 年 4 月发布，现在 Chrome 的内核是 Blink。Blink 其实是 webkit 的分支，如今大部分国产浏览器最新版都采用 Blink 内核（极速）。

来源：四川大学飞扬俱乐部《维修手册》PDF 第 115-123 页（印刷页 114-122）。
