# 新增文章、板块与维护方案

## 结论

当前架构不局限于维修知识。Markdown 文档目录、MkDocs 导航和按 `doc_id` 关联的问答都可以服务于任意主题。新增板块不需要改问答数据库或重新设计主题。

当前已使用 GitHub 仓库 https://github.com/fyscu/wiki 管理文章和源码，并通过 Pull Request 进行内容审核和自动构建检查。网页文章管理后台和生产自动发布尚未接入。Answer 管理员的审核权限针对问答，不自动代表 Wiki 文章编辑权限；GitHub 编辑权限由仓库协作者权限决定。

## 当前存储与展示

| 内容 | 位置或规则 |
| --- | --- |
| 文章正文 | `docs/` 下的 Markdown 文件 |
| 板块和左侧目录 | `mkdocs.yml` 的 `nav` |
| 页面右侧目录 | 从文章标题层级自动生成 |
| 图片 | `docs/images/`，可以按主题分目录 |
| 搜索索引 | MkDocs 构建时自动生成，支持中文 |
| 文章关联问答 | 稳定 `doc_id` 对应 Answer 的 `doc-<id>` 标签 |
| 内容负责人 | front matter 的 `owners`，目前是元数据，不是权限控制 |
| 发布版本 | 独立静态版本目录，切换 `index` 链接上线 |

`docs/handbook/` 只是现有维修内容目录，并不是必须使用的固定分类。`manual_pages` 也只是手册来源信息，新主题可以不填写。

## 新增文章

例如新增软件使用文章 `docs/software/windows-update.md`。先生成一次稳定文档 ID：

```powershell
node --input-type=module -e "import {ulid} from 'ulid'; console.log(ulid().toLowerCase())"
```

文章模板如下，`doc_id` 替换为上述生成值，不要每次更新都重新生成：

```markdown
---
title: Windows 更新问题处理
description: 常见更新失败的排查与恢复。
doc_id: <生成的26位小写ULID>
owners: [software-team]
tags: [Windows, 软件使用]
updated: 2026-09-07
---

# Windows 更新问题处理

## 适用范围

## 操作步骤

## 常见问题

## 参考资料
```

将文章加入 `mkdocs.yml` 导航后，就会进入相应板块。带有效 `doc_id` 的文章自动获得“本文问答”和“针对本文提问”，新文章不需要额外编写 Vue 页面。

当前链接规则要求文件路径使用小写英文字母、数字和连字符，例如 `software/windows-update.md`；页面标题、导航名和正文可以使用中文。未填写 `doc_id` 的普通页面仍能展示，但不进入文章问答关联清单。

## 新增板块

建议保留现有维修知识，按内容量逐步增加软件使用、编程与开发、网络基础、俱乐部事务等一级板块，避免初期产生大量空目录。一级 `nav` 对应顶部板块，嵌套项对应左侧目录。

例如创建 `docs/software/index.md`，再将以下片段合并进现有 `nav`：

```yaml
  - 软件使用:
      - 软件使用概述: software/index.md
      - Windows 更新问题处理: software/windows-update.md
```

以后还可以在板块内继续划分 Windows、Linux、办公软件等子目录。仅修改目录和文章就能实现，无需改后端路由。

## 当前发布步骤

1. 修改文章、图片和导航；运行 `npm run content:check` 校验文档 ID 及必需元数据。
2. 使用本机 `http://127.0.0.1:5173/` 预览。当前监听器会重建文章内容，但修改 `mkdocs.yml` 后应重新运行构建或重启预览进程。
3. 执行 `npm run build`。新增带 `doc_id` 的文章，还需通过 SSH 转发执行 `npm run qa:setup`，为新文档创建关联标签。
4. 执行 `node scripts/package-site.mjs` 并发布静态包，完整命令见 `DEPLOYMENT.md`。
5. 检查新文章、目录、搜索结果和“针对本文提问”。普通内容更新不需要重启 Answer 或 MySQL。

注意：`content:check` 当前会更新文章清单文件；它不是完整的拼写、失效链接和导航完整性检查。`qa:setup` 会创建缺失标签，但不会自动修改已存在标签的显示名称。

## 更新与移动

- 修改正文：保留 `doc_id`，更新日期和来源，重新构建发布；问答关系保留。
- 修改文章标题：同步导航显示名；如果希望 Answer 标签显示名也同步，需要补充标签更新操作。
- 移动或重命名文件：保留 `doc_id`，并配置旧文章 URL 的重定向。稳定 ID 能保住问答关联，但不能自动让旧网址继续可用。
- 删除文章：先决定是否归档及如何处理旧链接；已有讨论通常应保留并标注原文已归档。
- 图片更新：建议使用明确名称或新文件名，避免浏览器缓存旧图。

## 便利性提升

源码和文章现已纳入 `fyscu/wiki` 的 Git 版本管理。推送到 `main` 或提交 Pull Request 会运行 `Wiki Checks`：单元测试、文章元数据检查、代理配置一致性检查、完整构建，以及保存 7 天的静态预览包。正式服务器不会随推送自动更新。

| 方案 | 编辑体验 | 接入工作与限制 |
| --- | --- | --- |
| GitHub 网页编辑 / github.dev | 改正文和图片最直接，不用配置本机环境 | 已可使用，仍需维护 Markdown、稳定 ID 和导航；通过 PR 检查后人工发布 |
| Git 仓库 + 合并后自动发布 | GitHub 网页编辑后提交审核，合并即可上线 | 自动检查已建立；生产发布任务、发布授权和文章标签同步尚待接入 |
| 同域 Decap CMS + Git | 表单维护标题、分类、图片，Markdown 编辑和预览 | 可复用开源 CMS；通常使用 GitHub/GitLab 身份，不能直接把 Answer 当作身份提供方 |
| 自建同域文章管理后台 | 可直接复用 Wiki 管理员登录与角色 | 需要实现内容权限、草稿版本、审阅、构建任务与回滚，开发和维护成本更高 |

推荐近期采用“GitHub 网页编辑 → PR 审核 → 自动检查 → 维护者发布”，先让成员开始维护内容。小改动直接使用 GitHub 编辑按钮；同时编辑多篇文章或目录可在仓库页按 `.` 进入 github.dev，操作步骤见 `CONTRIBUTING.md`。

接下来补上合并后自动发布，再为频繁编辑的非技术成员增加 [Decap CMS](https://decapcms.org/) 表单后台。公开页面继续使用 OI Wiki 原主题，编辑端可放在同域 `/editor/`；当前 `/admin/` 被代理配置阻断，不应直接沿用它。Decap 的 GitHub 后端还需 OAuth 认证服务和组织授权；GitHub CLI 已登录不等于网页 CMS 已获得授权。

CMS 需要少量适配：`doc_id` 自动生成、必需元数据校验、分类目录维护、图片上传、发布状态，以及审核后触发构建。为避免编辑人员碰到主题配置和 MkDocs 的 Python YAML 标签，建议把可编辑导航拆成独立的普通 YAML 文件，再由构建钩子读入。

推荐的最终流程为：新建文章 → 编辑与预览 → 保存草稿 → 提交审核 → 合并版本 → 自动校验和创建文章标签 → 构建发布 → 失败时保留原版本。预览构建不接触生产发布凭据。

维护便利性优先级建议：先自动生成文章模板和文档 ID、校验导航，再自动同步问答标签和发布；最后接入网页编辑器和独立分类管理。仓库中已有版本管理和 CI，本次未创建新板块、接入 CMS 或启用生产自动发布。
