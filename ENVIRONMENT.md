# 环境交接记录

**更新：本文件以下内容是首版环境快照。2026-09-07 已使用 OI Wiki 原主题正式部署到 https://wiki.feiyang.ac.cn/，登录、问答和审核整合在 Wiki 内；审核插件已启用。飞书 SMTP 已完成收件箱投递确认，正式注册和密码找回已开放。当前入口与操作方式以 README.md、DEPLOYMENT.md 为准。**

日期：2026-09-07。当前阶段是基础环境搭建，不是完整产品上线。

## 已运行的资源

| 项目 | 当前状态 |
| --- | --- |
| 本机开发前端 | http://127.0.0.1:5173/，VitePress 热更新 |
| 服务器静态预览 | http://127.0.0.1:9081/，通过 SSH 转发 |
| 服务器问答 | http://127.0.0.1:9080/，通过 SSH 转发 |
| 服务器 | `45.40.247.178`，使用 `ubuntu` SSH 账号 |
| 问答容器 | `feiyang-wiki-answer`，仅绑定服务器 `127.0.0.1:9080` |
| 数据库 | 现有 MySQL 8.4.3 内的独立库 `feiyang_wiki_qa`，35 张表 |
| 数据库账号 | `fy_wiki_qa`，权限仅覆盖新库 |
| 资源限制 | Answer 512 MiB 内存、0.75 CPU，日志 10 MiB × 3 |
| 前端代理 | 仅公开问题列表 GET/HEAD，不转发 Cookie 和 Authorization |

本机 SSH 转发关闭后，两个服务器入口无法直接从本机打开；通过 `scripts/connect-server.ps1` 重新连接。

管理员凭据位于 `runtime/credentials.json`，不包含在 Git 或静态构建中。新服务器资源的配置路径和操作方法见 README。

## 验证结果

- 前端构建成功；npm 依赖审计为 0 项漏洞。
- 5 项单元测试通过：文档 ID、预填编码、采纳状态、代理凭据隔离和错误处理。
- 6 项浏览器测试通过：桌面/移动端的页面、真实 API 和管理员登录。
- 桌面和移动端截图已检查，文章插图及俱乐部标识正常加载。
- 服务器静态站与本机开发站的健康检查均通过。
- Answer 重启后，管理员账号和已创建文章标签仍存在。
- 原有 5 个业务容器保持运行；官网、FOC、学院、维修站、实验室站点均返回 200。
- 旧宿主机 MySQL 仍为 inactive/masked；系统盘仍约 53%。

## 镜像来源

官方 Linux amd64 Answer 2.0.2 发行包经 SHA-256 验证后，打包为本项目运行镜像。

```text
官方版本 commit: 3b9f1370612e690a0b7f230f05e688930db4c6d3
Linux 发行包 SHA256: 4724dccf336acc7885d1f7206700dd22869562be5dbd8b89de83fe82d09efe25
本次运行镜像 config digest: sha256:c4b93d26cdccb567c6b437d511a589a1d86426915d92571ca79dd87efeddd2eb
本次镜像导出包 SHA256: 6c47b388db3ca8db7b65709b6c16ea7274ff13087b9c44004032ecd50585d222
```

本机与服务器的 Docker Hub 连接均失败，因此采用官方发行包封装。没有使用来源不明的镜像代理，也没有改动 Docker 全局网络设置。

## 后续功能阶段

1. 构建并验证 reviewer-basic 等审核插件。目前运行镜像未包含审核插件，不能对外宣称审核已开启。
2. 接入 SMTP、注册验证及适用的账号方案。
3. 配置正式域名与 HTTPS，再决定公网开放时间。
4. 增加新库与附件的备份、恢复演练和正式发布回滚流程。
5. 在环境稳定后完善文档与问答交互，再逐步整理维修手册等内容。

原手册没有完整导入 Wiki。目前只有一篇用于检验文章路由、插图、来源标注和问答关联的样例。
