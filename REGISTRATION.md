# 注册与飞书邮件接入

## 现有账号

初始化管理员用户名为 `feiyang-admin`，登录邮箱为 `wiki-admin@feiyang.local`。随机密码保存在本机受保护文件 `runtime/credentials.json` 的 `admin_password`，不写入本文件。该邮箱为占位地址，不能收信；它与 SMTP 发件邮箱是两个独立设置。生产测试账号已按精确身份标记软删除。

## 本次注册流程

采用 Answer 的邮箱密码注册和激活链接，不另建账号数据库。用户提交用户名、邮箱和密码后，账号处于未验证状态；通过邮件链接验证后才能发布内容。重新发送的链接使旧链接失效，验证链接和重置链接默认有效期为 10 分钟，用过后不能重复使用。

统一主题页面包括 `/register/`、`/login/`、`/users/account-activation/`、`/users/password-reset/`。已补充重发倒计时、返回原页面、跨标签页激活、图形验证码接口和限流错误提示。发送邮件接口在正式代理中按 IP 限流；客户端倒计时只改善交互，不替代服务器限制。验证码界面跟随后台插件返回的状态显示；当前生产镜像未启用图形验证码插件。

## 可选方式

| 账号验证方式 | 特点 | 当前选择 |
| --- | --- | --- |
| 邮箱密码注册，点击激活链接 | Answer 原生支持，后续用密码登录 | 已实现 |
| 邮箱验证码注册 | 在页面输入短期数字验证码，需要增加对应后端能力 | 未采用 |
| 无密码邮件链接登录 | 每次登录依赖邮箱收信，需要相应认证插件或独立身份服务 | 未采用 |

| 邮件发送方式 | 适用情况 |
| --- | --- |
| 企业邮箱 SMTP | 可复用飞书，接入简单，需遵守企业邮箱发信限额 |
| 事务邮件服务 SMTP/API | 腾讯云 SES、阿里云邮件推送、Amazon SES 等，适合更大规模，通常有域名验证、用量配额及费用；SMTP 可直接接入，专有 HTTP API 需适配 |
| 自建邮件服务器 | 需要维护 IP 信誉、反向解析、反垃圾和投递，不建议当前阶段采用 |

本项目已确定使用飞书企业邮箱 SMTP。

## 需要用户完成

1. 在飞书中选定实际发件邮箱，建议使用专门的公共邮箱，如 `wiki@feiyang.ac.cn`，不要把个人密码用于服务器。
2. 公共邮箱：有邮箱权限的管理员进入 **管理后台 > 产品设置 > 邮箱 > 地址管理 > 公共邮箱 > 编辑 > IMAP/SMTP 服务 > 开启**，取得该邮箱的 SMTP 登录配置。
3. 若使用成员邮箱：成员在飞书客户端进入 **个人头像 > 设置 > 邮箱 > 第三方邮箱客户端登录 > 立即设置**，生成设备专用密码。组织策略不允许第三方客户端时，需要邮箱管理员先开放相应权限。
4. 将发件邮箱、SMTP 用户名和专用密码填入受保护的 `runtime/smtp.feishu.json`，只向开发人员提供文件路径；指定一个由你管理的测试收件箱，并确认测试邮件到达情况。

这些步骤涉及当前未授权访问的飞书管理会话、组织权限和凭据生成，因此需要你或飞书邮箱管理员完成。服务商实际发信配额也应在组织控制台确认。DNS 修改和服务器部署不需要你手工完成。

## 固定配置

已根据 [飞书官方 SMTP 指南](https://www.feishu.cn/hc/zh-CN/articles/221905160046) 核对：

- SMTP 主机：`smtp.feishu.cn`
- 端口：`465`
- 加密：`SSL`
- 认证：启用，用户名和专用密码使用飞书提供的值
- 也支持 `587 + STARTTLS`，当前配置选择 465

飞书要求 SPF 授权包含 `_netblocks.m.feishu.cn`。若域名已有 SPF，合并到原记录，不新建第二条 SPF。DKIM 使用飞书控制台给出的 selector 和记录值；DMARC 应结合现有发信业务确定策略，不能直接覆盖现有设置。

本次已确认 `feiyang.ac.cn` 存在 `v=spf1 +include:_netblocks.m.feishu.cn -all`，服务器连接 `smtp.feishu.cn:465` 的 TLS 证书校验成功。未修改邮箱 DNS 记录。

## 开发人员完成

先校验配置，命令不会打印密码，也不会发信：

```powershell
node scripts/configure-smtp.mjs --config runtime/smtp.feishu.json
```

接入 SSH 回环管理接口后写入配置：

```powershell
node scripts/configure-smtp.mjs --config runtime/smtp.feishu.json --apply
```

用户指定测试收件人后，显式添加 `--test-recipient <实际收件邮箱>` 可提交测试邮件。Answer 异步发信，接口成功只表示请求已接受，不能当作邮件送达。配置脚本不会自动开放注册。

接下来检查 SMTP TLS、认证和投递，核对 SPF/DKIM/DMARC，再用真实邮箱验证注册、激活、密码重置与垃圾邮件归类。完成后开启后台注册开关，保持邮箱验证必需，并同步更新正式站的 `account-status.json` 与 `mail-gate.conf`，通过 Nginx 检查后 reload。

## 验证

`npm run test:mail` 会启动独立 SQLite Answer 实例和只监听回环地址的 SMTP 收件器；仅接收 `@wiki-mail.test` 测试地址，不向外部发邮件。验证真实邮件 HTML 中的激活及重置链接、未验证账号拒绝发布、重发后旧链接失效、链接仅用一次、跨标签页激活、密码替换与旧密码失效。

飞书 SMTP 已写入服务器。2026-09-07 17:30（北京时间）已向用户指定的测试收件箱提交测试邮件，Answer 日志记录发送成功，用户随后确认邮件进入 Gmail 收件箱。

已执行 `node scripts/set-registration.mjs --enable`，正式注册、重发验证和密码找回均已开放，邮箱验证保持必需。该操作会备份原设置，并在 Nginx 配置检查失败时恢复；关闭入口使用 `--disable`。开放后的 4 项正式站桌面/手机账号页面和权限检查已通过，无新增测试邮件或测试账号。

## 通知邮件

Answer 原生的问题和回答路径已在 `lib/answer-links.mjs` 定义兼容规则，由 `scripts/nginx-api.mjs` 生成到正式代理配置；本机预览使用相同规则。退订入口为 `/users/unsubscribe?code=...`，只在用户点击确认后调用退订接口。隔离测试验证真实生成的回答通知邮件、回答定位和退订后对应通知通道关闭。

## 用户名登录

已部署“邮箱或用户名 + 密码”登录，原密码和账号 ID 保留，未迁移数据库。实际用户名可在右上角用户菜单查看；显示昵称不作为独立登录字段。登录去除输入前后的空白，用户名优先匹配原值，并兼容注册时生成的小写用户名。

后端继续使用原 `e_mail` 请求字段以兼容既有调用方，扩展该字段的校验和账号查找；密码校验、账号状态检查、会话签发、审核权限及限流保持原有实现。未验证账号仍须完成邮件验证后才能发布内容，停用或删除账号不能借用户名登录绕过限制。邮箱继续用于注册验证和密码找回。

最小源码补丁保存在 `patches/answer-username-login.patch`，镜像为 `feiyang/answer-runtime:2.0.2-login-b43aa249b983`，包含原官方审核插件。20 项正式站浏览器测试、8 项隔离登录及邮件流程测试、12 项前端/代理单元测试和 Go 登录/注册策略测试均已通过。
